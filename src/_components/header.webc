<slot><h1>Header</h1></slot>

<style webc:scoped>
    :host {
        padding-top:20px;
        padding-bottom:20px;
        background-color:var(--c-header-bg);
        box-shadow: var(--c-box-shadow);
    }
</style>